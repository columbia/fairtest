Usage:

obtain synsets n09636339 (Black people) and n09638875 (White people) from ImageNet (registration required).

Run 

./overfeat_dir/bin/XXX/overfeat n09636339/*.JPEG
./overfeat_dir/bin/XXX/overfeat n09638875/*.JPEG

and store results in 'results_black.txt' and 'results_white.txt'

* Use 'parse_results.py' to merge the two files
* Use 'find_labels.py' to group all images with a particular label